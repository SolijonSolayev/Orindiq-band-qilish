{
  "name": "orindiq-band-qilish",
  "version": "1.0.0",
  "description": "Moslashuvchan o'rindiqlarni bron qilish interfeysi prototipi / Sugarland Theatres Seat Booking Prototype",
  "main": "server.js",
  "scripts": {
    "dev": "npm run build && node server.js",
    "build": "esbuild src/app.ts --bundle --outfile=public/bundle.js --minify",
    "start": "node server.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "esbuild": "^0.28.2",
    "express": "^4.21.2",
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
